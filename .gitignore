*.iml
.gradle/
# The Supabase CLI arrives through `npx supabase`, which installs it here. The lockfile beside it
# is what pins the version; this is the copy it unpacks to.
node_modules/
__pycache__/
/local.properties
/.idea/
.DS_Store
/build
/app-android/build
/app-desktop/build
/core/build
/ui/build
.kotlin/
/captures
.externalNativeBuild
.cxx
local.properties
# Release keys stay out of the repository — they arrive through the CADENCE_* secrets.
# Where .github/scripts/build.sh stages the finished artefacts, locally as well as on a runner.
/dist/

*.keystore
*.jks
# …but the debug key is checked in on purpose: it is what makes one build installable
# over the last one. See the signing comment in app-android/build.gradle.kts.
!/app-android/debug.keystore

# Converted Todoist exports — real task data, and this repo is public (tools/README.md)
/cadence-backup.json
/cadence-import/
tools/cadence-backup.json
tools/cadence-import/
